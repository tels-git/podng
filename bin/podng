#!/usr/bin/perl -w

# This is a commandline wrapper around the Pod::PodNG Perl module

use strict;
use warnings;
use Getopt::Long;
use Pod::PodNG;

my $opt = { verbose => 0, debug => 0 };

my $podng = Pod::PodNG->new();

# TODO:
# * option handling
# * let Pod::PodNG parse the input
# * output the result
